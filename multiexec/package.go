// Package multiexec allows bundling multiple programs within a single
// binary. The individual programs are selected by the basename with which
// they were called. Thus, by linking the single binary as different names,
// the bundled program can be selected.
package multiexec
